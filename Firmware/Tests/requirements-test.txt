# Test dependencies for Mothbox
# Install on Raspberry Pi with: pip3 install -r requirements-test.txt

pytest>=7.0.0
pytest-mock>=3.10.0
pytest-timeout>=2.1.0
pytest-cov>=4.0.0
pytest-xdist>=3.5.0  # Parallel test execution
pytest-split>=0.9.0  # Deterministic test splitting for CI sharding

# Flask dependencies (required by integration tests that import app.py)
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SocketIO==5.3.6
Flask-WTF==1.2.1
Flask-Limiter==3.5.0
python-socketio==5.11.0

# Image quality analysis
scipy>=1.9.0  # For Laplacian edge detection in image quality tests
Pillow>=9.0.0  # Image processing (required by test_camera_stream.py)
numpy>=1.20.0  # Numerical operations (required by test_camera_stream.py)
opencv-python-headless>=4.5.0  # Computer vision operations (required by liveview_stream.py for focus peaking)

# Performance monitoring (Feature Set 1: Stream Stability tests)
psutil>=5.9.0  # For memory usage monitoring in long-running tests

# Flask web framework dependencies (for integration tests)
flask-wtf>=1.0.0  # CSRF protection for Flask app

# Image metadata handling
exif>=1.3.0  # EXIF metadata manipulation for capture scripts
piexif>=1.0.0  # GPS EXIF metadata embedding (required for GPS EXIF feature)

# Tag autocomplete
rapidfuzz>=3.0.0  # Fuzzy string matching for tag autocomplete (Issue #124)
