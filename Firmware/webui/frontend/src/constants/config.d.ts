/**
 * Type declarations for constants/config.js
 *
 * Provides TypeScript types for the JS config module during the
 * gradual migration to TypeScript. Add types here as .tsx components
 * import additional constants from config.js.
 */

export declare const Z_INDEX: {
  PHOTO_CONTROLS: string
  DROPDOWN: string
  TOOLBAR: string
  MODAL: string
  MAP_CONTROLS: string
  MAP_POPUP: number
}
