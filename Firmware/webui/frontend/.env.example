# API URL (optional - defaults to http://localhost:5000/api)
VITE_API_URL=http://localhost:5000/api

# Backend environment mode (development or production)
# Set to 'development' for local testing with debug features
# Set to 'production' for deployment (default if not set)
MOTHBOX_ENV=development
